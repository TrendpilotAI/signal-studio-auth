# BRAINSTORM — signal-studio-auth
_Generated by Judge Agent v2 (2026-03-02)_

## Current State Summary
FastAPI auth service bridging Supabase Auth with legacy ForwardLane JWTs. Core flows (signup/login/refresh/logout/me/invite-to-org) complete with dual-mode JWT middleware, in-memory rate limiting, 3 SQL migrations (organizations, user_profiles, RLS), and user mapping layer. TODO-351 (hardcoded secrets) and TODO-352 (in-memory rate limiting) are **already merged**. Completeness ~70% — functional but missing production hardening and enterprise features.

---

## 1. New Features (Revenue/Value Impact)

### 🔴 HIGH — Role-Based Access Control (RBAC) Dependency [M]
- Add `require_role(*roles)` FastAPI dependency using JWT `app_metadata.role` claims
- Currently `/invite-to-org` has **zero admin check** — any authenticated user can invite
- Enable per-endpoint role gates: `admin`, `analyst`, `viewer` tiers
- **Revenue impact:** Unblocks enterprise sales — orgs need permission isolation
- Effort: **M**

### 🔴 HIGH — Password Reset / Change Routes [S]
- Proxy `POST /auth/recover` → Supabase `/auth/v1/recover` (send reset email)
- Proxy `POST /auth/change-password` → Supabase admin update user
- Required for self-service onboarding without Supabase dashboard access
- Effort: **S**

### 🔴 HIGH — OAuth Provider Routes (Google, Microsoft) [M]
- Proxy Supabase OAuth flow: `/auth/oauth/google`, `/auth/oauth/microsoft`
- Return PKCE challenge + redirect URL to frontend
- Critical for enterprise SSO (Microsoft Entra) used by ForwardLane customers
- Effort: **M**

### 🟡 MEDIUM — Refresh Token Rotation + Revocation [M]
- Current `/refresh` passes token through but doesn't rotate or track revocation
- Add server-side refresh token revocation list (Redis SET with TTL)
- Rotate refresh token on each use (sliding expiry)
- Effort: **M**

### 🟡 MEDIUM — Org-Scoped API Keys [L]
- Generate long-lived API keys for service-to-service calls
- Store hashed key in `user_profiles` or separate `api_keys` table
- Validate in middleware alongside JWT auth
- Effort: **L**

### 🟢 LOW — Session Management Endpoint [S]
- `GET /auth/sessions` — list active sessions for current user
- `DELETE /auth/sessions/{session_id}` — revoke a specific session
- Effort: **S** (Supabase admin API has this)

---

## 2. Code Quality

### 🔴 HIGH — httpx Connection Pooling [S]
- Every route creates `async with httpx.AsyncClient()` — new TCP connection per request
- Replace with module-level `httpx.AsyncClient` instance (lifespan management via FastAPI)
- 10-50ms latency reduction per auth call
- Effort: **S**

### 🟡 MEDIUM — Pydantic v2 Migration [S]
- `requirements.txt` pins `pydantic>=1.10.0` — v2 has breaking changes but better performance
- Migrate models: `class Config` → `model_config`, `.dict()` → `.model_dump()`
- Effort: **S**

### 🟡 MEDIUM — Typed Response Models [S]
- Routes return raw `resp.json()` dicts — no response validation or OpenAPI schema
- Add `LoginResponse`, `SignupResponse`, `UserResponse` Pydantic models
- Improves API docs, catches Supabase API changes early
- Effort: **S**

### 🟢 LOW — DRY: Extract Supabase HTTP Helper [S]
- `_supabase_headers()` + `httpx.AsyncClient` pattern repeated in every route
- Extract `SupabaseClient` helper class with typed methods: `signup()`, `login()`, `invite()`
- Effort: **S**

---

## 3. Testing

### 🔴 HIGH — Integration Tests vs Supabase Local Stack [L]
- Zero integration tests — only unit tests with mocked httpx
- Use `supabase/postgres` Docker image + Supabase CLI for local dev stack
- Test full flow: signup → login → refresh → logout with real JWT round-trips
- Effort: **L**

### 🔴 HIGH — Coverage Gaps: /me, /logout, /refresh, /invite-to-org [M]
- `test_auth.py` doesn't exist (only `test_security.py` covers rate limiting + secrets)
- Need tests for all 6 auth routes, dual-mode middleware, ForwardLane fallback
- Effort: **M**

### 🟡 MEDIUM — Load Tests for Rate Limiter [S]
- Verify rate limiter holds under concurrent requests (threading edge cases)
- Use `locust` or `pytest-benchmark` + `asyncio.gather`
- Effort: **S**

---

## 4. Integrations

### 🔴 HIGH — Redis for Production Rate Limiting [M]
- Current in-memory limiter resets on restart and doesn't work across replicas
- Replace `_RateLimiter` with Redis sliding-window using `redis-py` + `aioredis`
- Configurable via `REDIS_URL` env var, falls back to in-memory if absent
- Effort: **M**

### 🟡 MEDIUM — Sentry Error Tracking [S]
- No error tracking in production
- Add `sentry-sdk[fastapi]` with DSN from env var
- Capture auth failures, JWT errors, upstream Supabase timeouts
- Effort: **S**

### 🟡 MEDIUM — Prometheus Metrics Endpoint [S]
- Add `/metrics` endpoint with `prometheus-fastapi-instrumentator`
- Track: auth latency p50/p95/p99, login success/failure rates, rate limit hits
- Effort: **S**

### 🟢 LOW — Email Service for Invite Notifications [S]
- Current invite flow sends magic link but no branded email
- Add Resend/SendGrid wrapper for custom invite email templates
- Effort: **S**

---

## 5. Workflows

### 🔴 HIGH — GitHub Actions CI Pipeline [S]
- No CI — changes go untested until manual run
- Add `.github/workflows/ci.yml`: pytest + ruff lint + bandit security scan + safety check
- Run on PR + main branch push
- Effort: **S**

### 🟡 MEDIUM — Pre-commit Hooks [S]
- Add `.pre-commit-config.yaml`: ruff, black, mypy, bandit
- Prevent weak secrets from landing (scan for `INSECURE_DEFAULTS` patterns)
- Effort: **S**

### 🟡 MEDIUM — Dockerfile + Railway Deploy Config [S]
- No deployment artifacts — can't ship to Railway/Fly without manual setup
- Add `Dockerfile` (python:3.11-slim), `railway.json` with health check
- Effort: **S**

---

## 6. Performance

### 🔴 HIGH — httpx Connection Pool (see Code Quality #1) [S]
- Repeated connection overhead is the top perf issue

### 🟡 MEDIUM — Async JWT Decode [S]
- `pyjwt.decode()` is synchronous — runs in async context blocking event loop
- Wrap in `asyncio.get_event_loop().run_in_executor()` or use `python-jose` async variant
- Effort: **S**

### 🟢 LOW — /me Response Caching [S]
- `/me` re-decodes JWT on every call (already decoded by middleware)
- Cache user dict in `request.state` — no re-decode needed (already done via `request.state.user`)
- Actually this is already correct — middleware sets `request.state.user`, `/me` just returns it
- No work needed ✓

---

## 7. Security

### 🔴 CRITICAL — Admin Check on /invite-to-org [S]
- Any authenticated user can invite anyone to any org
- Must verify `request.state.user.organization.id == body.organization_id` AND caller role is `admin`
- Effort: **S**

### 🔴 HIGH — Refresh Token Rotation (see New Features #4) [M]

### 🟡 MEDIUM — CORS Configuration [S]
- No CORS middleware visible in the codebase
- Add `CORSMiddleware` with allowlist: Signal Studio frontend domain + localhost dev
- Effort: **S**

### 🟡 MEDIUM — PKCE for OAuth Flows [M]
- When OAuth providers are added, must implement PKCE (code_verifier/challenge)
- Prevent authorization code interception attacks
- Effort: **M** (part of OAuth feature work)

### 🟢 LOW — Security Headers [S]
- Add `X-Content-Type-Options`, `X-Frame-Options`, `Strict-Transport-Security` via middleware
- Effort: **S**

---

## Priority Matrix

| Priority | Item | Effort |
|----------|------|--------|
| 🔴 P0 | Admin check on /invite-to-org | S |
| 🔴 P0 | Redis rate limiter | M |
| 🔴 P0 | GitHub Actions CI | S |
| 🔴 P1 | RBAC dependency injection | M |
| 🔴 P1 | Password reset/change routes | S |
| 🔴 P1 | httpx connection pooling | S |
| 🔴 P1 | Test coverage: all routes | M |
| 🔴 P1 | Refresh token rotation | M |
| 🟡 P2 | OAuth providers (Google/Microsoft) | M |
| 🟡 P2 | Integration tests vs Supabase local | L |
| 🟡 P2 | Pydantic v2 migration | S |
| 🟡 P2 | Response models | S |
| 🟡 P2 | Sentry integration | S |
| 🟡 P2 | Prometheus metrics | S |
| 🟡 P2 | CORS middleware | S |
| 🟡 P2 | Dockerfile + Railway config | S |
| 🟢 P3 | Org-scoped API keys | L |
| 🟢 P3 | Session management endpoints | S |
| 🟢 P3 | Pre-commit hooks | S |
| 🟢 P3 | Security headers middleware | S |
