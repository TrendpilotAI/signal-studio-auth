# BRAINSTORM — signal-studio-auth
_Generated by Judge Agent v2 (2026-03-01)_

## Current State Summary
Supabase auth migration module for Signal Studio/ForwardLane backend. Dual-mode JWT middleware,
FastAPI auth routes, RLS migrations, user mapping layer, and test suite. Core is ~70% complete —
functional but missing production-hardening and enterprise features.

---

## 1. New Features (Revenue/Value Impact)

### 🔴 HIGH — Role-Based Access Control (RBAC) [M]
- Add `require_role(*roles)` FastAPI dependency using JWT claims
- Enforce `admin` check on `/auth/invite-to-org` (currently any authenticated user can invite)
- Enable per-endpoint role gates for analyst/viewer/admin tiers
- **Revenue impact:** Unblocks enterprise sales — orgs need permission isolation

### 🔴 HIGH — Password Reset / Forgot Password [S]
- Proxy Supabase `/auth/v1/recover` (send reset email)
- Proxy `PUT /auth/v1/user` (update password with recovery token)
- Required for any production login flow

### 🔴 HIGH — ForwardLane User Migration Script [M]
- `scripts/migrate_users.py` reads FL Postgres, creates Supabase users via admin API
- Sets `app_metadata.legacy_user_id` for UUID→int mapping continuity
- Dry-run mode + idempotent (skip existing emails)

### 🟡 MEDIUM — MFA / TOTP Support [M]
- Expose Supabase MFA enrollment and verification endpoints
- Required for SOC2 compliance and enterprise deals

### 🟡 MEDIUM — SSO / SAML [L]
- Proxy Supabase OAuth/SAML config for enterprise IdP (Okta, Azure AD)
- Huge unlocker for Fortune 500 prospects

### 🟡 MEDIUM — Audit Logging [S]
- Log all auth events (login, logout, failed attempt, invite, password reset) to structured log
- Ship to Datadog / Loki / Postgres audit table
- Required for compliance

### 🟢 LOW — Email Verification Enforcement [S]
- Check `email_confirmed_at` in JWT claims
- Return 403 with "Please verify your email" if unverified

---

## 2. Code Quality

### 🔴 HIGH — Hardcoded Default Secret [CRITICAL SECURITY]
- `config/supabase_config.py` line ~35: `"very_secure_secret"` as fallback for `FORWARDLANE_JWT_SECRET`
- Must raise `ValueError` if `SUPABASE_JWT_SECRET` is empty in production

### 🟡 MEDIUM — DRY: _supabase_headers() duplication
- `routes/auth_routes.py` builds headers in multiple places; already extracted to helper — good
- But `service=True` vs `access_token=` parameter logic is fragile; add explicit validation

### 🟡 MEDIUM — Missing Return Type Annotations
- All route functions and middleware helpers lack return type hints
- Add `-> JSONResponse`, `-> dict[str, Any]`, etc.

### 🟢 LOW — _compat.py User model Config class
- Uses deprecated Pydantic v1 `orm_mode=True` and `allow_population_by_field_name`
- Migrate to Pydantic v2: `model_config = ConfigDict(from_attributes=True, populate_by_name=True)`

---

## 3. Testing

### 🔴 HIGH — No Integration Tests for Routes
- `tests/test_auth.py` tests middleware in isolation but doesn't test actual route handlers
- Add `TestClient` tests for `/auth/signup`, `/auth/login`, `/auth/logout`, `/auth/me`, `/auth/invite-to-org`
- Mock `httpx.AsyncClient` for Supabase API calls

### 🟡 MEDIUM — Missing Edge Case Tests
- Test empty `Authorization` header (malformed, no space)
- Test token with missing `aud` claim in dual mode
- Test `invite-to-org` with non-admin caller (should fail after RBAC fix)
- Test `_uuid_to_int` with empty string, malformed UUID

### 🟡 MEDIUM — No Migration Script Tests
- Once migration script exists, need unit tests for FL→Supabase metadata mapping

### 🟢 LOW — No Load/Stress Tests
- Auth endpoints are hammered at scale; add locust/k6 baseline

---

## 4. Integrations

### 🔴 HIGH — Rate Limiting (slowapi + Redis) [S]
- `routes/auth_routes.py` `/auth/login` and `/auth/signup` have no rate limiting
- Add `slowapi` with Redis backend: 5 req/min per IP for login, 10/hr for signup
- Prevents brute-force and credential stuffing

### 🟡 MEDIUM — Webhook: Auth Events → Internal Bus [M]
- Emit Supabase webhook events (user.created, password.reset) to internal event bus
- Enables downstream automation (welcome emails, CRM sync, Slack notifications)

### 🟡 MEDIUM — Sentry Error Tracking [S]
- Add `sentry-sdk[fastapi]` for auth failure monitoring
- Alert on spike in 401s / 403s

---

## 5. Workflows

### 🔴 HIGH — CI/CD: GitHub Actions [S]
- Add `.github/workflows/test.yml`: install deps → run pytest → coverage report
- Block PRs with failing tests

### 🟡 MEDIUM — Pre-commit Hooks [S]
- `ruff` for linting, `mypy` for type checking, `bandit` for security scanning
- Add `pyproject.toml` with tool configs

### 🟡 MEDIUM — Docker Compose for Local Dev [S]
- `docker-compose.yml` with Supabase local stack + app service
- One-command local dev environment

---

## 6. Performance

### 🟡 MEDIUM — JWT Decode Called Twice in Dual Mode [S]
- `supabase_auth_middleware` decodes token twice in dual mode (once unverified, once verified)
- Cache the unverified decode result and pass to `_is_supabase_token`

### 🟡 MEDIUM — httpx Client Not Reused [S]
- Each auth route creates a new `httpx.AsyncClient()` per request
- Use a module-level `AsyncClient` with connection pooling (lifespan context)

### 🟢 LOW — Add Response Caching for /auth/me [S]
- `/auth/me` could cache user dict in Redis for ~60s to reduce JWT decode overhead at scale

---

## 7. Security

### 🔴 CRITICAL — Rate Limiting Missing (see #4)

### 🔴 HIGH — invite-to-org Missing Admin Check
- Any authenticated user can invite anyone to any org
- Fix: add `require_role("admin")` dependency

### 🔴 HIGH — SUPABASE_JWT_SECRET Empty String Fallback
- Empty secret means all unsigned tokens pass validation
- Must fail fast on startup if secret is empty

### 🟡 MEDIUM — CORS Configuration Missing
- No CORS middleware configured; defaults to allow-all in some FastAPI setups
- Add explicit `CORSMiddleware` with allowlist of Signal Studio domains

### 🟡 MEDIUM — Token Logging Risk
- Ensure tokens are never logged (check logger.warning calls pass exc not token)
- Audit all log statements for PII leakage

### 🟢 LOW — Security Headers [S]
- Add `X-Content-Type-Options`, `X-Frame-Options`, `Strict-Transport-Security` via middleware

---

## Priority Order

1. Rate limiting (CRITICAL security gap)
2. Admin role check on invite-to-org (HIGH security gap)
3. Password reset routes (blocks production launch)
4. User migration script (blocks Supabase cutover)
5. RBAC dependency (blocks enterprise features)
6. CI/CD setup (blocks safe iteration)
7. Fix hardcoded secrets (CRITICAL, easy fix)
8. httpx connection pooling (performance)
9. MFA support (enterprise requirement)
10. Audit logging (compliance)
