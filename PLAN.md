# PLAN — signal-studio-auth
_Generated by Judge Agent v2 (2026-03-02)_

## Architecture Overview

```
Signal Studio Frontend
        │
        ▼
signal-studio-auth (FastAPI)
    ├── POST /auth/signup    → Supabase /auth/v1/signup
    ├── POST /auth/login     → Supabase /auth/v1/token
    ├── POST /auth/refresh   → Supabase /auth/v1/token (refresh_token grant)
    ├── POST /auth/logout    → Supabase /auth/v1/logout
    ├── GET  /auth/me        → request.state.user (decoded by middleware)
    ├── POST /auth/invite-to-org → Supabase admin API
    └── Middleware: supabase_auth_middleware
            ├── SUPABASE mode → verify local JWT
            ├── FORWARDLANE mode → verify via ForwardLane service
            └── DUAL mode → try Supabase first, fallback ForwardLane
```

## Proposed Architecture Additions

```
                    ┌─────────────┐
                    │    Redis    │ ← Rate limiting + token revocation
                    └─────────────┘
                          │
signal-studio-auth (FastAPI)
    ├── /auth/recover        [NEW] Password reset
    ├── /auth/change-password [NEW] 
    ├── /auth/oauth/google   [NEW] OAuth provider
    ├── /auth/oauth/microsoft [NEW]
    ├── /metrics             [NEW] Prometheus
    ├── /health              [NEW] Health check
    └── RBAC middleware      [NEW] require_role() dependency
```

## Dependency Graph

```
TODO-353 (admin RBAC check) ← No deps — do first
TODO-402 (Redis rate limiter) ← No deps
TODO-403 (refresh token rotation) ← TODO-402 (Redis preferred)
TODO-354 (password reset routes) ← No deps
TODO-356 (RBAC dependency) ← TODO-353
TODO-357 (integration tests) ← All feature TODOs
TODO-358 (CI pipeline) → TODO-406 (Dockerfile)
TODO-404 (Pydantic v2) ← No deps
TODO-405 (CORS/Sentry/Prometheus) ← No deps
TODO-406 (Dockerfile + CI) ← No deps
OAuth providers ← TODO-356 (RBAC), TODO-354 (passwords done)
```

## Recommended Execution Order

### Phase 1 — Security & Production Hardening (P0, Week 1)
1. **TODO-353** — Admin RBAC check on /invite-to-org (S, ~2h) ← CRITICAL security gap
2. **TODO-402** — Redis rate limiter (M, ~5h) ← production multi-replica support
3. **TODO-403** — Refresh token rotation (M, ~5h) ← security requirement
4. **TODO-405** — CORS + Sentry + Prometheus (S, ~3h) ← needed for production deploy

### Phase 2 — Feature Completeness (P1, Week 2)
5. **TODO-354** — Password reset/change routes (S, ~3h)
6. **TODO-356** — RBAC dependency injection (M, ~4h)
7. **TODO-406** — Dockerfile + GitHub Actions CI (S, ~3h)
8. **TODO-404** — Pydantic v2 migration (S, ~3h)

### Phase 3 — Quality & Hardening (P2, Week 3)
9. **TODO-357** — Integration tests vs Supabase local (L, ~8h)
10. **TODO-359** — httpx connection pooling (S, ~2h)
11. **TODO-358** — CI pipeline enhancements (if not covered in TODO-406)
12. **TODO-224/355** — User migration script (M)

### Phase 4 — Enterprise Features (P3, Week 4+)
13. OAuth providers (Google, Microsoft)
14. Org-scoped API keys
15. Session management endpoints

## Risk Assessment

| Risk | Severity | Mitigation |
|------|----------|------------|
| Any authenticated user can invite to org | HIGH | Fix in TODO-353 immediately |
| In-memory rate limiter lost on restart | HIGH | TODO-402 Redis migration |
| No CI — bad code ships silently | HIGH | TODO-406 GitHub Actions |
| Stolen tokens can't be revoked | HIGH | TODO-403 revocation list |
| No CORS → frontend blocked in prod | MEDIUM | TODO-405 |
| Pydantic v1/v2 silent breakage | MEDIUM | TODO-404 |
| No integration tests → regressions | MEDIUM | TODO-357 |
| httpx per-request connection overhead | LOW | TODO-359 |

## Next Immediate Action
Run `TODO-353` — add the admin RBAC check to `/invite-to-org`. This is a **security vulnerability** in production right now.
